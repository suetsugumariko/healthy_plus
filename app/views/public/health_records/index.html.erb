
<p>体調記録表</p>

<p>体重</p>
<%= column_chart @health_records.group_by_month(:created_at, last: 7).sum(:body_weight)%>
<p>体温</p>
<%= column_chart @health_records.group_by_month(:created_at, last: 7).sum(:temperature)%>
<p>脈拍</p>
<%= column_chart @health_records.group_by_month(:created_at, last: 7).sum(:pulse)%>
<p>血圧</p>
<%= column_chart @health_records.group_by_month(:created_at, last: 7).sum(:max_blood_pressure)%>
<%= column_chart @health_records.group_by_month(:created_at, last: 7).sum(:min_blood_pressure)%>
<p>SPO2</p>
<%= column_chart @health_records.group_by_month(:created_at, last: 7).sum(:saturation)%>


<%#= line_chart @health_records.map { |health_record|
    {name: health_record.start_time, data: health_record.body_weight }
} %>
<%#= line_chart @health_records.map { |health_record|
    {name: health_record.start_time, data: health_record.temperature }
} %>
<%#= line_chart @health_records.map { |health_record|
    {name: health_record.start_time, data: health_record.pulse }
} %>
<%#= line_chart @health_records.map { |health_record|
    {name: health_record.start_time, data: health_record.max_blood_pressure }
} %>
<%#= line_chart @health_records.map { |health_record|
    {name: health_record.start_time, data: health_record.min_blood_pressure }
} %>
<%#= line_chart @health_records.map { |health_record|
    {name: health_record.start_time, data: health_record.saturation }
} %>